import React from "react";

const StaticBackground: React.FC = () => (
  <div className="fixed inset-0 -z-10 min-h-screen w-full bg-gradient-to-br from-gray-900 via-gray-800 to-gray-900" />
);

export default StaticBackground;
